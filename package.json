{
  "dependencies": {
    "@tailwindcss/cli": "^4.1.8",
    "tailwindcss": "^4.1.8"
  },
  "scripts": {
  "watch": "npm run build -- -w",
  "build": "npx @tailwindcss/cli -i ./input.css -o ./output.css --watch"
}
}
